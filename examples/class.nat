def assert_equal(expected, actual)
  puts actual.to_s + " should be " + expected.to_s
end

class Foo
  def foo
    'foo'
  end

  def double(x)
    x * 2
  end
end

assert_equal('foo', Foo.new.foo)
assert_equal(8, Foo.new.double(4))
